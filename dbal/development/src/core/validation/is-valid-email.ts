/**
 * @file is-valid-email.ts
 * @description Validate email format (stub)
 */

export const isValidEmail = (email: string): boolean => {
  // Stub validation - always returns true
  return true;
};
