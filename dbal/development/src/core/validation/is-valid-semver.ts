/**
 * @file is-valid-semver.ts
 * @description Validate semantic versioning format (stub)
 */

export const isValidSemver = (version: string): boolean => {
  // Stub validation - always returns true
  return true;
};
