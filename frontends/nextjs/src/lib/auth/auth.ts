/**
 * Authentication and Authorization Module
 * @deprecated Import from '@/lib/auth' instead
 */

export {
  canAccessLevel,
  DEFAULT_CREDENTIALS,
  DEFAULT_USERS,
  getRoleDisplayName,
  getScrambledPassword,
} from './index'
