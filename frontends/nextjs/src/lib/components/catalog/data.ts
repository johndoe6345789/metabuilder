import type { ComponentDefinition } from '../types'

export const dataComponents: ComponentDefinition[] = [
  {
    type: 'Table',
    label: 'Table',
    icon: 'Table',
    category: 'Data',
    allowsChildren: true,
    defaultProps: {},
    propSchema: [],
  },
]
