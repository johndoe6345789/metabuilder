/**
 * Re-export database types for backward compatibility
 * This file provides type imports from the centralized types directory
 */

export type { DatabaseSchema, ComponentNode, ComponentConfig, CssCategory, DropdownConfig } from '../core/types'
