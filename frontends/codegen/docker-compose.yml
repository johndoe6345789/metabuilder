version: '3.8'

services:
  db:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=codegen
      - POSTGRES_USER=codegen
      - POSTGRES_PASSWORD=codegen
    volumes:
      - codeforge-postgres:/var/lib/postgresql/data
    restart: unless-stopped

  frontend:
    build:
      context: ..
      dockerfile: codegen/Dockerfile
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
      - USE_FLASK_BACKEND=true
      - FLASK_BACKEND_URL=http://backend:5001
    depends_on:
      - backend
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'wget', '--quiet', '--tries=1', '--spider', 'http://localhost/health']
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.codeforge.rule=Host(`codeforge.example.com`)'
      - 'traefik.http.services.codeforge.loadbalancer.server.port=80'

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5001:5001"
    volumes:
      - codeforge-data:/data
    environment:
      - PORT=5001
      - DATABASE_URL=host=db port=5432 dbname=codegen user=codegen password=codegen
      - MIGRATIONS_DIR=/app/migrations
      - SQLITE_MIGRATION_PATH=/data/codeforge.db
    depends_on:
      - db
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:5001/health']
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

volumes:
  codeforge-data:
  codeforge-postgres:

networks:
  default:
    name: codeforge-network
